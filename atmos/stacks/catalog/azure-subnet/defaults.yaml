# stacks/catalog/azure-subnet/defaults.yaml

components:
  terraform:
    azure-subnet:
      metadata:
        component: azure-subnet
      vars:
        enabled: true
        delimiter: ""
        regex_replace_chars: "/[^a-zA-Z0-9]/"
        label_order:
          - namespace
          - environment
          - stage
          - name