components:
  terraform:
    azure-resource-group:
      metadata:
        component: azure-rsg
      vars:
        enabled: true
        delimiter: "-"
        regex_replace_chars: "/[^a-zA-Z0-9-]/"
        label_order:
          - namespace
          - environment
          - stage
          - name
          - attributes
