# stacks/catalog/azure-private-endpoint/defaults.yaml

components:
  terraform:
    azure-private-endpoint:
      metadata:
        component: azure-private-endpoint
      vars:
        enabled: true
        delimiter: ""
        regex_replace_chars: "/[^a-zA-Z0-9]/"
        label_order:
          - environment
          - stage
          - name
          - namespace
        is_manual_connection: false